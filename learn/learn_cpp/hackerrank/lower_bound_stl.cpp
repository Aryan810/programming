#include <iostream>
#include <vector>

using namespace std;

int* binary_search_vector(vector<int> vec, int item){
    int index=0;
    int* ret = new int(2);

    void search_binary(vector<int> v){
        if (v.at(v.size()/2) == item){
            index = v.size()/2;
        }
    }
}

int main(){

    return 0;
}